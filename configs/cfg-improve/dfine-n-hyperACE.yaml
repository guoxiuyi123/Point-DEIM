backbone:
  # [from, module, args]
  - [-1, StemBlock, [16, 16, True]] # 0-P2/4
  - [-1, HG_Stage, [16, 64, 1, 3, False, False, 3, True, 'se']] # 1-P2/4
  - [-1, HG_Stage, [32, 256, 1, 3, True, False, 3, True, 'se']] # 2-P3/8
  - [-1, HG_Stage, [64, 512, 2, 3, True, True, 5, True, 'se']]  # 3-P4/16
  - [-1, HG_Stage, [128, 1024, 1, 3, True, True, 5, True, 'se']] # 4-P5/32

encoder:
  - [3, ConvNormLayer_fuse, [128, 1, 1]] # 5-P4/16
  - [4, ConvNormLayer_fuse, [128, 1, 1]] # 6-P5/32

  - [-1, TransformerEncoderBlock, [8, 512, 0.1, "relu", 10000, False]] # 7-P5/32

  - [2, ConvNormLayer_fuse, [128, 1, 1]] # 8-P3/8

  - [[8, 5, 7], HyperACE, [128, 1, 8, True, True]] # 9-P4/16
  - [-1, DownsampleConv, []] # 10-P5/32

  - [[5, 9], FullPAD_Tunnel, []] # 11-P4/16
  - [[7, 10], FullPAD_Tunnel, []] # 12-P5/32

  - [-1, ConvNormLayer_fuse, [128, 1, 1]] # 13
  - [-1, nn.Upsample, [None, 2, "nearest"]] # 14
  - [[-1, 11], Concat, []] # 15-P4/16
  - [-1, RepNCSPELAN4, [128, 256, 21, 2, False, 'silu']] # 16-P4/16
  - [[-1, 9], FullPAD_Tunnel, []] # 17-P4/16

  - [-1, SCDown, [128, 3, 2, 'silu']] # 18-P5/32
  - [[-1, 13], Concat, []] # 19-P5/32
  - [-1, RepNCSPELAN4, [128, 256, 21, 2, False, 'silu']] # 20-P5/32
  - [[-1, 10], FullPAD_Tunnel, []] # 21-P5/32

decoder:
  - [[17, 21], DFINETransformer, {"feat_strides":[16, 32], "hidden_dim": 128, "num_levels": 2, "num_layers": 3, "num_points": [6, 6], "dim_feedforward": 512}]