backbone:
  # [from, module, args]
  - [-1, StemBlock, [16, 16, True]] # 0-P2/4
  - [-1, HG_Stage, [16, 64, 1, 3, False, False, 3, True, 'se']] # 1-P2/4
  - [-1, HG_Stage, [32, 256, 1, 3, True, False, 3, True, 'se']] # 2-P3/8
  - [-1, ESMoE, [128, 4, 2]] # 3-P3/8
  - [-1, HG_Stage, [64, 512, 2, 3, True, True, 5, True, 'se']]  # 4-P4/16
  - [-1, ESMoE, [256, 8, 2]] # 5-P4/16
  - [-1, HG_Stage, [128, 1024, 1, 3, True, True, 5, True, 'se']] # 6-P5/32
  - [-1, ESMoE, [512, 16, 2]] # 7-P5/32

encoder:
  - [3, ConvNormLayer_fuse, [256, 1, 1]] # 8-P3/8
  - [5, ConvNormLayer_fuse, [256, 1, 1]] # 9-P4/16
  - [7, ConvNormLayer_fuse, [256, 1, 1]] # 10-P5/32

  - [-1, TransformerEncoderBlock, [8, 1024, 0.1, "relu", 10000, False]] # 11-P5/32

  - [-1, ConvNormLayer_fuse, [256, 1, 1]] # 12
  - [-1, nn.Upsample, [None, 2, "nearest"]] # 13
  - [[-1, 5], Concat, []] # 14-P4/16
  - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 15-P4/16

  - [-1, ConvNormLayer_fuse, [256, 1, 1]] # 16
  - [-1, nn.Upsample, [None, 2, "nearest"]] # 17
  - [[-1, 3], Concat, []] # 18-P3/8
  - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 19-P3/8

  - [-1, SCDown, [256, 3, 2, 'silu']] # 20-P4/16
  - [[-1, 15], Concat, []] # 21-P4/16
  - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 22-P4/16

  - [-1, SCDown, [256, 3, 2, 'silu']] # 20-P5/32
  - [[-1, 11], Concat, []] # 23-P5/32
  - [-1, RepNCSPELAN4, [256, 512, 64, 1, False, 'silu']] # 24-P5/32

decoder:
  - [[19, 22, 24], DFINETransformer, {"feat_strides":[8, 16, 32], "hidden_dim": 256, "num_levels": 3, "num_layers": 3, "num_points": [3, 6, 3], "dim_feedforward": 1024}]